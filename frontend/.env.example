# ── aws_account_platform (frontend) — environment ──────────────────
# Copy to .env for local dev. The Next.js app is a pure frontend that
# calls the ASP.NET Core API; it holds no DB credentials or secrets.

# .NET backend API base URL (server-side only; never exposed to the browser).
API_BASE_URL="http://localhost:5247"
